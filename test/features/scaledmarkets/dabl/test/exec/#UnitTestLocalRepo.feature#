# language: en

@unit @exec
Feature: UnitTestLocalRepo
	
	@done
	Scenario: A single include pattern
		Given
		When 
		Then
	
	@done
	Scenario: A single exclude pattern
		Given
		When 
		Then
	
	@done
	Scenario: One include pattern, and exclude one file
		Given
		When 
		Then
	
	@done
	Scenario: One include pattern, and exclude files that match a specified extension
		Given
		When 
		Then
	
	@done
	Scenario: One include pattern, and exclude a directory
		Given
		When 
		Then
	
	@done
	Scenario: Two include patterns, and exclude files that match a specified extension
		Given there are files in each two directories that have a .txt extension
		When 
		Then
